# Qosmic audit harness — pinned dependencies (decision D8: reproducible env).
# Exact pins so the runtime behaves identically on any machine.
# Installed into .venv by `python tools/setup_env.py` before any crawl runs.

httpx==0.27.2            # HTTP client: robots/sitemap fetch, BFS discovery, tech checks
beautifulsoup4==4.12.3   # HTML parsing: link extraction, metadata
lxml==5.3.0              # fast parser backend for bs4 / XML
playwright==1.48.0       # headless browser: screenshots + rendered DOM (capture phase)
Pillow==10.4.0           # image inspection for image-optimization tech check

# --- dev / test ---
pytest==8.3.3            # test runner
respx==0.21.1            # httpx mocking for deterministic offline tests
